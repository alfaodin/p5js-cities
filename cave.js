function CaveGenerator() {

    this.linePositions = [];

    this.linePositions.push(createVector());

    while(true){

        var angle = ran
    }
  
}

SpaceShip.prototype.show = function() {
  stroke(255);
  strokeWeight(8);
  if (this.startEngine) {
    fill(205);
  } else {
    fill(255);
  }
  rect(this.pos.x, this.pos.y, 20, 20);
};
